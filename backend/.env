### for development use
# When running locally without Docker, use 'localhost' since PostgreSQL is exposed to host machine via port mapping (5432:5432)
# When running in Docker, use 'db' as defined in docker-compose.yml service name
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=admin
DB_DATABASE=nexplore

PORT=8001